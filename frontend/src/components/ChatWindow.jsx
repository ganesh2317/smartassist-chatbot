import {useEffect,useRef} from "react";import Message from "./Message.jsx";export default function ChatWindow({messages,loading,error}){const end=useRef(null);useEffect(()=>end.current?.scrollIntoView({behavior:"smooth"}),[messages,loading]);return <section className="chat-window" aria-live="polite">{messages.map((m,i)=><Message key={`${m.timestamp}-${i}`} message={m}/>)}{loading&&<div className="message-row bot"><div className="avatar">AI</div><div className="bubble">SmartAssist is typing…</div></div>}{error&&<p className="error-banner" role="alert">{error}</p>}<div ref={end}/></section>}
