import {useState} from "react";import {login,register} from "../services/chatbotApi.js";export default function LoginPage({onLoggedIn}){const [mode,setMode]=useState("login"),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const reg=mode==="register";async function submit(e){e.preventDefault();setLoading(true);setError("");try{const d=reg?await register(username.trim(),password):await login(username.trim(),password);onLoggedIn(d);}catch(err){setError(err.message||"Something went wrong.");}finally{setLoading(false)}}return <div className="login-shell"><form className="login-card" onSubmit={submit}><img src="/smartassist-logo.png" alt="SmartAssist"/><h1>SmartAssist</h1><p>{reg?"Create an account":"Log in to continue"}</p><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={reg?"new-password":"current-password"} placeholder="At least 8 characters"/></label>{error&&<p className="error-banner">{error}</p>}<button className="primary" disabled={loading}>{loading?"Please wait...":reg?"Create account":"Log in"}</button><button type="button" className="link" onClick={()=>{setMode(reg?"login":"register");setError("")}}>{reg?"Already have an account? Log in":"New here? Create an account"}</button></form></div>}
