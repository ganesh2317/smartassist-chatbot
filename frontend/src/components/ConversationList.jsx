export default function ConversationList({conversations,selectedId,onSelect,onNewChat,onDelete,open}){return <aside className={`history-sidebar ${open?"open":""}`}><div className="history-header"><h2>Chats</h2><button onClick={onNewChat}>New chat</button></div><ul>{!conversations.length&&<li className="empty">No chats yet.</li>}{conversations.map(c=><li className={c.id===selectedId?"selected":""} key={c.id}><button className="chat-title" onClick={()=>onSelect(c.id)}>{c.title||"New chat"}</button><button className="delete" onClick={()=>onDelete(c.id)}>Delete</button></li>)}</ul></aside>}
